# API URL Configuration
# For production HTTPS deployment, set this to your API's HTTPS URL
# e.g., https://api.51stock.com.cn (backend should be behind a reverse proxy with HTTPS)
# For local development, leave unset to use http://localhost:8000
NEXT_PUBLIC_API_URL=
